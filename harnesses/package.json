{
  "name": "@table-stakes/harnesses",
  "private": true,
  "version": "0.0.0",
  "description": "Standalone 3D dev harnesses. No backend, no room, no auth, no React shell.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "0.180.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
