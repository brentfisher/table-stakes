# recap-pre-reveal-teaser

Publish the viewer's own already-computed match result early (under `you`, never the rival's,
never outcome-revealing), add a client teaser that shows it progressively — ending in a
motion-gated score tally-up — before the full recap arrives, and shorten the results-phase wait
now that dead time is no longer needed to withhold the outcome.
